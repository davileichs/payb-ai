{
  "name": "WeatherTool",
  "description": "Get current weather information for a specific location. Always use metric units.",
  "parameters": {
    "type": "object",
    "properties": {
      "latitude": {
        "type": "number",
        "description": "The latitude of the location to get weather for"
      },
      "longitude": {
        "type": "number",
        "description": "The longitude of the location to get weather for"
      }
    },
    "required": ["latitude", "longitude"]
  }
}
