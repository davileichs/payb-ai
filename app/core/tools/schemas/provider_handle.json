{
  "name": "ProviderHandle",
  "description": "Switch between different AI providers (OpenAI, Ollama) and manage provider settings",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["switch", "list", "status", "test"],
        "description": "The action to perform: switch provider, list available providers, check current status, or test provider connection"
      },
      "provider": {
        "type": "string",
        "enum": ["openai", "ollama"],
        "description": "The AI provider to switch to (required for switch action)"
      },
      "model": {
        "type": "string",
        "description": "Specific model to use with the provider (optional)"
      }
    },
    "required": ["action"]
  }
}
